<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.school.mapper.HsAttendanceMapper">
    
    <resultMap type="HsAttendance" id="HsAttendanceResult">
        <result property="attendanceId"    column="attendance_id"    />
        <result property="studentId"    column="student_id"    />
        <result property="studentName"    column="student_name"    />
        <result property="attendanceDate"    column="attendance_date"    />
        <result property="status"    column="status"    />
        <result property="remark"    column="remark"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="deptName"    column="dept_name"    />
        <result property="studentNo"    column="student_no"    />
    </resultMap>

    <sql id="selectHsAttendanceVo">
        select a.attendance_id, a.student_id, a.student_name, a.attendance_date, a.status, a.remark, a.del_flag, a.create_by, a.create_time, a.update_by, a.update_time,
               u.user_name as student_no, d.dept_name
        from hs_attendance a
        left join sys_user u on a.student_id = u.user_id
        left join sys_dept d on u.dept_id = d.dept_id
    </sql>

    <select id="selectHsAttendanceList" parameterType="HsAttendance" resultMap="HsAttendanceResult">
        <include refid="selectHsAttendanceVo"/>
        <where>  
            <if test="studentId != null "> and a.student_id = #{studentId}</if>
            <if test="studentName != null  and studentName != ''"> and a.student_name like concat('%', #{studentName}, '%')</if>
            <if test="attendanceDate != null "> and a.attendance_date = #{attendanceDate}</if>
            <if test="params.beginAttendanceDate != null and params.beginAttendanceDate != ''"> and a.attendance_date &gt;= #{params.beginAttendanceDate}</if>
            <if test="params.endAttendanceDate != null and params.endAttendanceDate != ''"> and a.attendance_date &lt;= #{params.endAttendanceDate}</if>
            <if test="status != null  and status != ''"> and a.status = #{status}</if>
            <if test="deptId != null "> and u.dept_id = #{deptId}</if>
        </where>
    </select>
    
    <select id="selectHsAttendanceByAttendanceId" parameterType="Long" resultMap="HsAttendanceResult">
        <include refid="selectHsAttendanceVo"/>
        where a.attendance_id = #{attendanceId}
    </select>

    <insert id="insertHsAttendance" parameterType="HsAttendance" useGeneratedKeys="true" keyProperty="attendanceId">
        insert into hs_attendance
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="studentId != null">student_id,</if>
            <if test="studentName != null">student_name,</if>
            <if test="attendanceDate != null">attendance_date,</if>
            <if test="status != null">status,</if>
            <if test="remark != null">remark,</if>
            <if test="delFlag != null">del_flag,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="studentId != null">#{studentId},</if>
            <if test="studentName != null">#{studentName},</if>
            <if test="attendanceDate != null">#{attendanceDate},</if>
            <if test="status != null">#{status},</if>
            <if test="remark != null">#{remark},</if>
            <if test="delFlag != null">#{delFlag},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateHsAttendance" parameterType="HsAttendance">
        update hs_attendance
        <trim prefix="SET" suffixOverrides=",">
            <if test="studentId != null">student_id = #{studentId},</if>
            <if test="studentName != null">student_name = #{studentName},</if>
            <if test="attendanceDate != null">attendance_date = #{attendanceDate},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where attendance_id = #{attendanceId}
    </update>

    <delete id="deleteHsAttendanceByAttendanceId" parameterType="Long">
        delete from hs_attendance where attendance_id = #{attendanceId}
    </delete>

    <delete id="deleteHsAttendanceByAttendanceIds" parameterType="String">
        delete from hs_attendance where attendance_id in 
        <foreach item="attendanceId" collection="array" open="(" separator="," close=")">
            #{attendanceId}
        </foreach>
    </delete>

    <select id="selectAttendanceStats" parameterType="HsAttendance" resultType="java.util.Map">
        SELECT
            CASE a.status
                WHEN '1' THEN '正常'
                WHEN '2' THEN '迟到'
                WHEN '3' THEN '早退'
                WHEN '4' THEN '缺勤'
                WHEN '5' THEN '请假'
                ELSE '未知'
                END as name,
            COUNT(*) as value
        FROM hs_attendance a
        LEFT JOIN sys_user u ON a.student_id = u.user_id
        WHERE a.del_flag = '0'
        <if test="deptId != null "> and u.dept_id = #{deptId}</if>
        <if test="studentName != null  and studentName != ''"> and a.student_name like concat('%', #{studentName}, '%')</if>
        <if test="attendanceDate != null "> and a.attendance_date = #{attendanceDate}</if>
        <if test="params.beginAttendanceDate != null and params.beginAttendanceDate != ''"> and a.attendance_date &gt;= #{params.beginAttendanceDate}</if>
        <if test="params.endAttendanceDate != null and params.endAttendanceDate != ''"> and a.attendance_date &lt;= #{params.endAttendanceDate}</if>
        GROUP BY a.status
    </select>
</mapper>